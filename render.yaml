services:
  - type: web
    name: kural-api
    env: node
    buildCommand: npm install && npm run build:db
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8080
    healthCheckPath: /
    github:
      branch: main
      deploy_on_push: true
      repo: karuna-1001/kural
    autoDeploy: true